[package]
name = "raft"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = "0.4"
simple_logger = "1.16.0"
rand = "0.8.4"
parking_lot = { version = "0.5.2", features = ["deadlock_detection"] }